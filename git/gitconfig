[user]
    name = Hugo Baraúna
    email = hugo.barauna@plataformatec.com.br
[color]
    status = auto
    diff = auto
    branch = auto
[core]
    excludesfile = ~/.gitignore
    editor = vim
[alias]
    st = status -sb
    ci = commit
    co = checkout
    br = branch
    d  = diff
    dc = diff --cached
    timelog = log --pretty=format:'%Cred%h%Creset - %C(yellow)%ae%Creset - %Cgreen%cd%Creset - %s%Creset' --abbrev-commit --date=iso
    tree = log --graph --pretty=format:'%Cred%h%Creset - %C(yellow)%ae%Creset - %s%Creset - %Cgreen%cd%Creset'
#    hp = "!. ~/.githelpers && show_git_head"
#    l = "!. ~/.githelpers && pretty_git_log"
[github]
    user = hugobarauna
[codebase]
    username = hugobarauna
    domain = plataformatec.codebasehq.com
[clean]
    requireForce = false
[push]
    default = simple
[browser "gitscm"]
    cmd = /bin/sh -c 'open http://git-scm.com/docs/$(basename $1 .html)' --
[help]
    browser = gitscm
[credential]
    helper = /opt/boxen/bin/boxen-git-credential
[hub]
    protocol = https
[merge]
    ff = false
[branch]
    autosetuprebase = always
[pull]
    rebase = true
[filter "lfs"]
    clean = git lfs clean %f
    smudge = git lfs smudge %f
    required = true
